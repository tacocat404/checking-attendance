# 개발 로그 (DEVLOG)

## 2026-07-06

### [기획] UI/UX 초안 기능 추출 및 재설계 방향 확정
- **사용자 프롬프트(요약)**: UI/UX팀 초안이 만족스럽지 않아 팀장(프론트엔드)이 직접 다시 디자인하기로 함. 초안에 있는 기능을 추출하고, 불필요한 기능을 제거한 뒤 재설계 예정.
- **Claude의 변경사항**: `UI,UX/attendance-dashboard`의 6개 페이지(홈/출퇴근/근태내역/통계/이메일/설정)와 script.js 분석 후 기능 인벤토리 작성.
  - **유지**: 출근/퇴근 버튼(→ Google Forms 연동 예정), 주별·일별 출결 차트, 오늘의 지각·결근자 테이블, 근태내역 테이블, 메일 발송 이력 조회
  - **제거**: 설정 페이지 전체, 가짜 로그인 폼, 프론트 "AI 메일 생성" 버튼(Gemini는 Make 파이프라인 담당), 장식용 검색창·알림 버튼, Quick Menu, 하드코딩 AI 분석 카드, 사번 컬럼
  - 페이지 구조: 6페이지 → 4페이지(홈/출퇴근/근태내역/통계)로 축소 방향

### [기획] 새 디자인 시안 3종 제작
- **사용자 프롬프트(요약)**: 기획서대로 이메일 입력 + 출퇴근 버튼 → Google Forms 자동 제출, Google Sheets 데이터를 통계에 활용하는 방향으로 개발 시작. 새 산출물은 `프론트엔드 및 팀장` 폴더에. 디자인 시안 몇 가지 먼저 뽑아서 비교.
- **Claude의 변경사항**: `프론트엔드 및 팀장/디자인시안/`에 홈 대시보드 기준 시안 3종 제작 (브라우저에서 바로 열어 비교 가능, 더미 데이터).
  - `시안A_클린라이트.html` — 흰 배경 + 토스 느낌 블루 포인트, 상단 네비게이션, 깔끔한 카드형
  - `시안B_다크대시보드.html` — 다크 네이비 + 민트 포인트, 사이드바 레이아웃, 파이프라인 상태 표시
  - `시안C_웜미니멀.html` — 크림 배경 + 테라코타 포인트, 에디토리얼 스타일, 대형 인사말 히어로
- **다음 단계**: 시안 확정 → 4페이지(홈/출퇴근/근태내역/통계+메일이력) 실제 구현 → 출퇴근 버튼의 Google Forms 자동 제출(prefill/formResponse) 연동 → Google Sheets 데이터 연동(공개 시트 gviz JSON 또는 Apps Script 웹앱 방식 결정 필요)

### [기획] 레퍼런스 기반 시안 D 추가 (오케스트라 스타일)
- **사용자 프롬프트(요약)**: Orchestra(orchestra.bio) 웹사이트 스크린샷을 레퍼런스로 제시. 시니어 디자이너 관점에서 이 무드로 시안 제작 요청.
- **Claude의 변경사항**: `프론트엔드 및 팀장/디자인시안/시안D_포레스트그린.html` 제작.
  - 레퍼런스에서 가져온 요소: 딥 포레스트 그린(#0e2b25) 다크 히어로/푸터, 세리프 대형 헤드라인(Noto Serif KR), 민트 알약(pill) 버튼, 크림+모눈종이 질감 라이트 섹션, 다크 배경 위에 떠 있는 흰색 대시보드 카드, 기찻길(트랙) 장식 모티프, 대문자 아이브로우 라벨(GET STARTED TODAY)
  - 구성: 다크 히어로(출근/퇴근 체크 버튼 + 실시간 시계) → 플로팅 출결 현황 카드(KPI 4종 + 주간 트렌드 + 오늘 비율 도넛) → 라이트 섹션(지각자/결근자 테이블, 월별 출근율, 내 기록) → 다크 CTA 푸터
  - 로컬 프리뷰 서버(`.claude/launch.json`, python http.server 4173)로 렌더링 검증. 시계가 "16시 26분 8초"로 표시되던 문제를 발견해 4개 시안 전부 `HH:MM:SS` 포맷으로 수정.
- **다음 단계**: 시안 D 방향 확정 여부 확인 → 확정 시 이 스타일로 전체 페이지(홈/근태내역/통계/메일이력) 구현 + Forms/Sheets 연동 시작

### [기획] 시안 E 제작 — 뮤트 팔레트 + 통계·레이아웃 전면 재설계
- **사용자 프롬프트(요약)**: 시안 D가 레퍼런스 모방 수준이라 별로. 새 뮤트 팔레트(Golden Sand #CACF85 / Muted Olive #8CBA80 / Air Force Blue #658E9C / Dusty Grape #4D5382 / Vintage Grape #514663) 제시. 통계 설계 개선, 다크↔라이트로 페이지가 분리된 느낌 제거. 여러 턴 돌며 디자인 자체 점검 요청.
- **Claude의 변경사항**: `프론트엔드 및 팀장/디자인시안/시안E_뮤트팔레트.html` 신규 제작.
  - **분리감 해결**: 풀블리드 다크 섹션을 없애고 전체를 웜 아이보리(#f6f5ef) 단일 캔버스로. 다크 요소는 Vintage Plum 펀치카드(시계+출퇴근 버튼+지난주 기록) 하나로 응축
  - **팔레트 적용**: 정상=올리브, 지각=샌드, 결근=플럼, 정보/메일=블루로 상태 색상 체계화. 헤드라인 형광펜 하이라이트는 샌드
  - **통계 재설계**: 거대 도넛 제거 → ① 오늘 현황은 세그먼트 가로 바(18/3/2 비율이 폭으로 즉시 읽힘) ② KPI 4종은 스파크라인 미니차트 내장 카드 ③ 주간 현황은 23명 기준 누적 막대 ④ 개인 기록은 월간 캘린더 히트맵(6월 완성 데이터)
  - 검증 중 수정: `scroll-behavior:smooth`가 스크린샷 캡처를 막는 문제 제거, 히트맵 날짜 논리 오류(미래 날짜에 기록 표시, 6월 31일 존재) 수정, 주간 차트 부제("100% 기준"→"인원 수 기준") 정정, 펀치카드 중간 여백에 지난주 기록 스트립 추가
- **다음 단계**: 시안 E 확정 여부 확인 → 확정 시 서브페이지(근태 내역/통계/메일 이력) 동일 스타일로 구현 → Forms/Sheets 연동

### [기획] 시안 F 제작 — 에디토리얼 전면 전환 (탈템플릿)
- **사용자 프롬프트(요약)**: 시안 E도 히트맵·그래프·상단 네비 배치가 "어디서 본 디자인 + AI티"가 남. 라운드 줄이기, 홈에 기능 욱여넣지 말기, 스크롤하며 호흡이 긴 애니메이션, 독창성 개선 요청.
- **Claude의 변경사항**: `프론트엔드 및 팀장/디자인시안/시안F_에디토리얼.html` 신규 제작. 대시보드 문법을 버리고 신문/공문서 에디토리얼 문법으로 전환.
  - **라운드 제거**: border-radius 0 전체. 알약(pill) 뱃지 폐기 → ■ 사각 마커 + 텍스트 상태 표기
  - **헤더 독창성**: 로고+네비바 대신 신문 마스트헤드 — 발행일·동기화·이메일 상단 메타라인, 대형 제호 "출결기록실", 우측 "색인" 넘버링 목차(01 오늘/02 근태 내역/03 통계/04 메일 이력)
  - **홈 기능 다이어트**: 차트·히트맵·KPI 카드 전부 제거. 홈 = ① 대형 얇은 숫자 시계+사각 출퇴근 버튼 ② 문장형 통계("오늘 23명 중 18명이 정시에 도착했습니다" + 카운트업) + 플랫 세그먼트 바 ③ 지각·결근 통합 원장(ledger) 테이블 ④ 통계 페이지 예고 블록. 상세 차트는 통계 페이지로 위임
  - **스크롤 호흡**: 섹션 패딩 110~140px, IntersectionObserver 리빌(1.1s 이징+스태거), 숫자 카운트업, 세그먼트 바 scaleX 성장 애니메이션
  - 검증 중 수정: 시계 분(分) 하이라이트가 분리된 막대처럼 보이는 문제 → 하이라이트 제거하고 초(秒)만 회색 처리
- **다음 단계**: 시안 F 확정 여부 확인 → 확정 시 서브페이지 구현 + Forms/Sheets 연동

### [기획] 색감 3안 제작 (시안 F 기반)
- **사용자 프롬프트(요약)**: 시안 F 방향 확정. 사무실/출근 앱 느낌의 색감 초안 3개 요청.
- **Claude의 변경사항**: `프론트엔드 및 팀장/디자인시안/시안F_색감3안.html` — 우측 하단 스위처로 3색감 전환 비교. 가) 오피스 네이비, 나) 인주 스탬프(웜 페이퍼+차콜+도장 인주색 버튼), 다) 그라파이트 틸.
- **결과**: 사용자가 **"나" 인주 스탬프 확정**.

### [구현] 실제 사이트 개발 시작 — 출결기록실 v1
- **사용자 프롬프트(요약)**: "나" 색감으로 개발 시작. Make 파이프라인은 Forms(Watch Responses)→Sheets(Add Row)→Gemini→Gmail→Sheets(로그) 구조. 구글 폼 입력: 출근/퇴근 선택 + 시간 + 이메일. 시트 구조는 추후 전달 예정.
- **Claude의 변경사항**: `프론트엔드 및 팀장/출결기록실/` 사이트 구축 (시안 F 레이아웃 + 인주 스탬프 색감).
  - `index.html` (01 오늘) — 시계+출퇴근 버튼(Forms 제출), 오늘 문장형 통계+플랫 바, 지각·결근 원장, 통계 예고
  - `history.html` (02 근태 내역) — 전체 기록 원장 + 상태/날짜/이름 필터
  - `stats.html` (03 통계) — 요약 지표, 일별 플랫 바 10일, 개인별 누적 테이블(분포 미니 바)
  - `mail.html` (04 메일 이력) — 발송 대장 + 구분/상태/이름 필터
  - `js/config.js` — **폼 ID·entry 번호·시트 ID를 채우는 유일한 파일** (비개발자용 주석 포함). 비어 있으면 데모 모드
  - `js/data.js` — 데이터 계층. 시트(gviz JSON) 읽기 + 데모 데이터 폴백. 시트 구조 확정 시 `parseSheetRows()`만 수정
  - `js/app.js` — 시계/리빌/이메일 저장(localStorage)/Forms 제출(POST formResponse, 시간형 질문 `_hour/_minute` 지원)
  - 검증: 4페이지 렌더·필터·검색, 출근 플로우(이메일 미설정 에러→설정→데모 제출→상태 갱신), "나" 팔레트 적용을 프리뷰에서 확인
- **다음 단계**: ① 백엔드팀에게 폼 ID + entry 번호 3개 받아 config.js에 입력 ② 시트 구조 확정되면 parseSheetRows() 수정 + 시트 링크 공개 설정 ③ 실데이터로 통합 테스트

### [구현] Google Forms 실연동 완료
- **사용자 프롬프트(요약)**: 실제 폼 주소와 시트 주소(탭 3개) 전달.
- **Claude의 변경사항**:
  - 폼 공개 페이지의 FB_PUBLIC_LOAD_DATA_를 직접 파싱해 질문 구조 확인 — 폼 "출결 관리 시스템"은 5개 질문: 이름(entry.761764073), 이메일(entry.77041659), 출퇴근 구분 라디오 "출근"/"퇴근"(entry.898363687), 출퇴근 시간·'시간' 유형(entry.235496377), 이번달 지각·결근 횟수(entry.1839426182, 필수 아님·미전송). 모든 질문 필수 아님 확인
  - `config.js`에 실제 폼 ID + entry 번호 기입. 시트 ID(1uoqE2qP…)와 탭 gid 3개도 기록
  - 폼에 '이름' 질문이 있어 사이트에 **이름 저장 기능 추가** (이메일과 동일한 인라인 편집, localStorage) — app.js 프로필 편집기 일반화, index.html에 "내 이름" 줄 추가, 이름 미설정 시 제출 차단
  - **검증**: fetch를 가로채는 방식으로 실발송 없이 전송 데이터 확인 — formResponse URL, 이름/구분(출근)/시·분 분리/이메일 모두 정확
  - 데모 띠 문구 수정: "출퇴근 버튼은 실제 기록됩니다. 숫자·명단은 아직 데모 데이터"
- **미해결**: 시트가 비공개(401)라 열 구조 읽기 불가. 시트 "공유 → 링크가 있는 모든 사용자 → 뷰어" 설정 필요. 공개되면 탭 3개 용도 파악 후 parseSheetRows() 작성
- **주의**: 이제 사이트의 출근/퇴근 버튼을 누르면 **실제 폼에 기록되고 Make 파이프라인(메일 발송 포함)이 작동**함

### [구현] Google Sheets 실데이터 연동 완료
- **사용자 프롬프트(요약)**: 시트 공개 설정 완료("됐어").
- **Claude의 변경사항**:
  - 시트 3개 탭 구조 직접 확인 — 88862461=폼 응답(타임스탬프/이름/이메일/구분/시간/횟수), 276345751=판정·발송 로그(Make판정/VBA판정/일치여부/Gemini문구), 1061791224=VBA 판정 입력(아직 빈 시트)
  - `data.js` 실데이터 파서 재작성:
    - gviz 셀 형식 대응 — 타임스탬프는 "2026. 7. 6 오후 9:30:26"(f)과 epoch 초(v) 혼재, 시간은 "Date(1899,11,30,9,20,0)" 직렬 형식 → 정규식 파싱
    - 폼 응답은 출근/퇴근 "사건" 단위 → (날짜,이름)으로 묶어 하루 기록으로 변환, 출근시간 > 09:00이면 지각 판정(백엔드 판정의 근사치, 주석 명시)
    - 이름 없는 행(Make가 쓴 빈 행) 필터링
    - 발송 로그 → 메일 이력 변환 (Gemini 문구가 있으면 제목으로 표시, VBA판정·일치여부도 보존 — 추후 비교 화면용)
  - 검증: 실데이터 renders — 홈 "오늘 1명 중 0명 정시 도착", 원장 "지각 김민준 09:20 +20분 발송됨 21:42", 내역 1건, 메일 2건, 통계 일별 1행
  - 통계 카운트업이 데이터 주입 전에 발동해 0으로 멈추는 버그 수정 (`animateCounters()` 추가, 렌더 후 재실행)
  - config.js 탭 주석을 확정 용도로 갱신, vbaGid 추가
- **다음 단계**: VBA 판정 시트에 데이터가 쌓이면 "판정 일치율 비교" 화면(기획서 5·6장 지표) 추가 검토. 백엔드팀에 Make "Add a Row"가 이름 없는 빈 행을 쓰는 문제 전달 필요

### [구현] 프로필 입력 UX 개선 — 이름·이메일 한 번에 저장
- **사용자 프롬프트(요약)**: 이름·이메일 입력칸이 한 번에 저장 안 되는 느낌.
- **원인**: 기존은 이름·이메일이 각각 클릭→인라인 편집→개별 저장 방식이라 두 번 조작 필요, 저장 여부도 불명확.
- **Claude의 변경사항**: 홈 옆 패널에 **프로필 카드** 신설.
  - 두 입력칸(이름/이메일) + **저장 버튼 한 세트**. 저장 시 둘 다 함께 검증·저장, 어느 칸에서 Enter를 쳐도 동시 저장
  - 이메일 형식 검증 추가(정규식) — 빈 이름/잘못된 이메일이면 저장 거부하고 기존값 유지
  - 저장 후엔 "이름/이메일 표시 + 수정 버튼" 뷰로 전환, 수정 클릭 시 다시 입력폼(기존값 채워짐)
  - 마스트헤드 이메일은 클릭 편집 제거하고 표시 전용(`data-mast-email`, 대문자)으로 정리 — 4개 페이지 공통
  - app.js: `bindInlineEditor` 제거 → `bindProfileEditors`/`showProfileView`로 폼 방식 재작성
  - **검증**(프리뷰 자동화): 초기 폼 표시 → 입력 후 저장 → 뷰 전환 + localStorage 저장 확인 → 출근 제출 payload에 이름·이메일 정상 포함 → 수정 재진입 → 잘못된 이메일 거부 및 기존값 보존까지 확인

### [배포] GitHub 저장소 생성 및 푸시
- **사용자 프롬프트(요약)**: DEVLOG·기획서 참고해 정리하고 GitHub에 푸시.
- **결정**: 범위 = 출결기록실 폴더만 / 공개 = Public.
- **Claude의 변경사항**:
  - 배포 전 민감정보 스캔 — 백엔드 blueprint에 API 키·토큰·웹훅 시크릿 **없음**(팀원 이메일 1개만). config.js의 폼·시트 ID는 노출되나 시트가 원래 링크 공개 상태라 추가 노출 미미. README에 공개 저장소 주의 문구 명시
  - `README.md`(프로젝트 개요·파이프라인·화면·설정·팀 구성), `.gitignore` 작성. `docs/DEVLOG.md`를 저장소 안에 복사
  - `출결기록실/`를 저장소 루트로 `git init -b main` → 11개 파일 첫 커밋
  - 사용자가 GitHub에서 저장소(`checking-attendance`) 생성 → 원격 연결 후 push 완료
  - **저장소**: https://github.com/tacocat404/checking-attendance
- **다음 단계**: (선택) GitHub Pages 배포로 실제 접속 주소 생성 / 백엔드팀에 Make 빈 행·VBA 시트 이슈 전달

### [배포] 공개 사이트 개인정보 보호 — 위치 기반 데모/실데이터 분기
- **사용자 프롬프트(요약)**: 배포된 공개 사이트가 실제 개인정보(실명·이메일)를 노출함. 발표용 데모 데이터로 전환 요청.
- **원인 정리**: 더미 데이터는 `index.html`에 박힌 게 아니라 `data.js`의 "시트 못 읽을 때 폴백"이었음. 시트 ID 입력+공개 설정 이후 로컬·배포 모두 실데이터를 읽게 되어 공개 사이트에도 실명·이메일이 표시됨.
- **Claude의 변경사항**:
  - `data.js`에 `isLocalEnvironment()` 추가 — protocol=file: 또는 hostname이 localhost/127.0.0.1/빈값일 때만 true
  - `loadData()`에서 **로컬일 때만 실제 시트를 읽고, 그 외(github.io 등 공개 주소)에서는 자동으로 데모 데이터만 표시** (fail-closed: 실데이터는 명시적으로 로컬에서만)
  - index 데모 안내 문구 수정: "예시 데이터 — 개인정보 보호를 위해 공개 화면에는 데모 표시. 출퇴근 버튼은 실제 Forms 기록"
  - **검증**: localhost → demo:false(실데이터 김민준), 판별식 시뮬레이션에서 localhost/127.0.0.1/file=실데이터, github.io/임의도메인=데모 확인
- **남은 고려**: 출퇴근 버튼 제출은 위치와 무관하게 실제 폼으로 전송됨(폼 자체가 공개라 원래 가능). 공개 사이트에서 아무나 제출하는 걸 막으려면 별도 처리 필요 — 사용자 결정 대기
