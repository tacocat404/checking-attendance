/* ============================================================
   설정 파일 — 연동 정보는 전부 여기에만 있습니다.
   ============================================================ */
const CONFIG = {

  /* ── Google Forms (연동 완료 · 2026-07-06) ──
     폼: "출결 관리 시스템" — 질문 5개
     ① 이름(단답) ② 이메일(단답) ③ 출퇴근 구분(라디오: 출근/퇴근)
     ④ 출퇴근 시간('시간' 유형) ⑤ 이번달 지각·결근 횟수(단답, 미전송)   */
  FORM: {
    id: "1FAIpQLSfLlvgeh8Kskdc97LSLxAzWuwkQ3aLSIir7R1-MsYqBV4a1eA",
    fields: {
      name:  { entry: "entry.761764073" },
      email: { entry: "entry.77041659" },
      type:  { entry: "entry.898363687" },
      time:  { entry: "entry.235496377", kind: "time" },
      /* count: entry.1839426182 — '이번달 지각·결근 횟수'는 필수 아님.
         나중에 시트 데이터로 자동 계산해 채울 예정이라 지금은 보내지 않음 */
    },
    typeValues: { in: "출근", out: "퇴근" },   // 폼 라디오 보기와 정확히 일치 (확인됨)
  },

  /* ── Google Sheets (연동 완료 · 2026-07-06, 링크 공개 확인) ──
     탭 용도 확인됨:
     · 88862461  = 폼 응답 (타임스탬프/이름/이메일/구분/시간/횟수) → 출결 기록
     · 276345751 = 판정·발송 로그 (Make판정/VBA판정/일치여부/Gemini문구) → 메일 이력
     · 1061791224 = VBA 판정 입력 (날짜/학번/이름/VBA최종판정) → 추후 비교 기능용 */
  SHEET: {
    id: "1uoqE2qPSP_erqrfTIgSuars42pZIbLpeDqEbdRyalE0",
    gid: "88862461",          // 폼 응답 탭
    mailGid: "276345751",     // 판정·발송 로그 탭
    vbaGid: "1061791224",     // VBA 판정 탭 (아직 미사용)
  },

  /* ── 근무 규칙 ── */
  WORK: {
    start: "09:00",           // 출근 마감(이후 도착 = 지각) — 백엔드 판정 기준과 일치해야 함
    end: "18:00",
  },
};
